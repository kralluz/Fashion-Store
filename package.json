{
  "name": "fashion-store",
  "version": "1.0.0",
  "private": true,  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "start:api": "node scripts/api-server.js",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up",
    "docker:dev": "docker-compose up --build",
    "docker:stop": "docker-compose down"
  },
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-icons": "^5.5.0",
    "express": "^4.18.2"
  }
}
